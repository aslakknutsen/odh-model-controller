apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
    name: example-onnx-mnist
    namespace: ns-cnzjw
    labels:
        inferenceservice-name: example-onnx-mnist
spec:
  gateways:
  - opendatahub/odh-gateway
  hosts:
  - '*'
  http:
  - match:
    - uri:
        prefix: /modelmesh/wisdom/
    rewrite:
      uri: /
    route:
    - destination:
        host: modelmesh-serving.ns-cnzjw.svc.cluster.local
        port:
          number: 8008